{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "preinstall": "sh -c 'rm -f package-lock.json yarn.lock; case \"$npm_config_user_agent\" in pnpm/*) ;; *) echo \"Use pnpm instead\" >&2; exit 1 ;; esac'",
    "start": "./start-app.sh",
    "dev": "./start-app.sh",
    "build": "pnpm run typecheck && pnpm --filter @workspace/flight-sim run build",
    "typecheck": "pnpm --filter @workspace/flight-sim run typecheck",
    "test": "pnpm --filter @workspace/flight-sim run test"
  },
  "private": true,
  "devDependencies": {
    "typescript": "~5.9.3"
  }
}
